extends ../layout 
block content
    h2 Ваши результаты по тестам:
        ol(class="list-group list-group-numbered")
            each result in results
                li(class="list-group-item")
                    span Тест ##{result.test_id} - #{result.result_points} балла(ов)
                

